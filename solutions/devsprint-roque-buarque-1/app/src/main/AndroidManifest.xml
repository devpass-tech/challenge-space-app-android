<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devpass.spaceapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpaceApp"
        android:name=".application.SpaceApp">
<!--        <meta-data-->
<!--            android:name="com.google.android.geo.API_KEY"-->
<!--            android:value="${GOOGLE_MAPS_API_KEY}" />-->
        <activity
            android:name=".presentation.next.NextLaunchesActivity">
        </activity>
        <activity
            android:name=".presentation.rocket.RocketDetailsActivity">
        </activity>
        <activity
            android:name=".presentation.launchdetails.LaunchMapDetailActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>